apiVersion: v1
kind: Service
metadata:
  name: vistula-app-service
spec:
  type: NodePort
  selector:
    app: vistula-app
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080 # This is the permanent door number we are assigning it